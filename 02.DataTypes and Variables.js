/* DataTypes : 
Null, undefined, number, string, symbol, number and objects
*/

// Variable with var keyword can be used anywhere in the program
// Variable with let keyword can only be used within the particular scope
// Variable with const keyword can never be changed

var greet = "Welcome";
console.log(greet);

var nums;   // Declaring Variable
nums = 0;   // Assigning Variable
console.log(nums);

nums = 100;
console.log("Nums changed to " + nums);
